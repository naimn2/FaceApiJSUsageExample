doctype html
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(http-equiv="X-UA-Compatible", content="IE=edge")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title= title
        //- script(src="https://cdn.jsdelivr.net/npm/@tensorflow/tfjs")
        //- script(src="https://cdn.jsdelivr.net/npm/@tensorflow-models/mobilenet")
        //- script(src="https://cdn.jsdelivr.net/npm/@tensorflow-models/knn-classifier")
        //- script(src="https://cdn.jsdelivr.net/npm/@tensorflow-models/blazeface")

        //- face-api js
        script(src="/js/face-api.min.js") 

        //- face-mesh
        //- script(src="https://cdn.jsdelivr.net/npm/@tensorflow-models/facemesh")
        //- script(src="https://unpkg.com/@tensorflow/tfjs-core@2.1.0/dist/tf-core.js")
        //- script(src="https://unpkg.com/@tensorflow/tfjs-converter@2.1.0/dist/tf-converter.js")

        //- You must explicitly require a TF.js backend if you're not using the tfs union bundle. -->
        //- script(src="https://unpkg.com/@tensorflow/tfjs-backend-wasm@2.1.0/dist/tf-backend-wasm.js")
        style.
            body {
                padding: 80px;
            }
            button {
                padding: 10px;
            }
            #console{
                padding: 10px;
            }
            video{
                padding: 10px;
            }
    body 
        h1= title
        #console
        video#webcam(autoplay playsinline muted width="224" height="224")
        br
        div(style="padding: 10px;")
        button#addMe(style="padding: 10px;") Add Me
        br
        input#inputLabel(type="text", name="Input Label" style="padding: 10px;")
        button#addLabel(style="padding: 10px;") Add Class

        script(src="/js/script.js")